# SlimEmail Agent API — copy thành .env local (không commit .env)
SLIMEMAIL_API_BASE=https://user.slimemail.vn/api/agent
SLIMEMAIL_API_KEY=
SLIMEMAIL_BRAND_ID=542
SLIMEMAIL_BRAND_NAME=APLUS

# Local dev:
# SLIMEMAIL_API_BASE=http://localhost/slimemail/api/agent
